# .env.example
#
# SETUP:
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Edit .env.local with your actual values
#
# The .dev.vars symlink (committed to repo) points to .env.local, so both
# development modes read from the same file:
# - Vite development: pnpm dev (reads .env.local)
# - Cloudflare Workers: pnpm prod (reads .dev.vars → .env.local)
#
# PRODUCTION:
# - Set as encrypted secrets in Cloudflare dashboard
# - Workers & Pages → wemeditate-web → Settings → Environment variables

# =============================================================================
# SERVER-SIDE ONLY (Never exposed to browser)
# =============================================================================

# SahajCloud (formerly PayloadCMS) API Key
SAHAJCLOUD_API_KEY=

# Server-side Sentry DSN (for server error tracking)
# Create separate Sentry project for server errors
SENTRY_DSN=

# =============================================================================
# BROWSER-ACCESSIBLE (Embedded in client bundles at build time)
# =============================================================================

# SahajCloud GraphQL endpoint URL
# Development default: http://localhost:3000
# Production: Set in wrangler.toml or Cloudflare dashboard
PUBLIC__SAHAJCLOUD_URL=http://localhost:3000

# Mapbox API token for LocationSearch component
# Even though "PUBLIC", this is still YOUR secret (restrict by domain)
PUBLIC__MAPBOX_ACCESS_TOKEN=

# Client-side Sentry DSN (for browser error tracking)
# Create separate Sentry project for client errors
PUBLIC__SENTRY_DSN=
