{
  "$comment": "Per-repo values read by the sydevs `workflow` plugin and its skills. Values only \u2014 the rules that consume them live in the skills, the stories in docs/.",
  "packageManager": "pnpm",
  "leanGate": {
    "command": ".claude/skills/pr-prep/check.sh",
    "full": ".claude/skills/pr-prep/check.sh --full",
    "contents": "lint + tsc --noEmit + test:run"
  },
  "contractStep": {
    "description": "Consume the generated types from SahajCloud main. No migrations here.",
    "command": "pnpm types:cms"
  },
  "securityReview": {
    "triggerPattern": "^server/|^pages/preview/|^wrangler\\.toml|^vite\\.config\\.ts|^sentry\\.[a-z]+\\.config\\.ts|^scripts/|^\\.env|^\\.dev\\.vars",
    "rationale": "Server-side rendering, preview routes, and the deploy/build configuration."
  },
  "worktreeSetup": [
    "pnpm install"
  ],
  "worktreeNotes": "Copy .env / .dev.vars from the main checkout if the branch needs them.",
  "deployTarget": "Cloudflare Workers (web) + Cloudflare Pages (Ladle)",
  "devServer": {
    "command": "pnpm dev --port {port}",
    "basePort": 5173,
    "healthPath": "/",
    "isolateDatabase": false
  },
  "generatedFiles": [
    {
      "pattern": "^server/payload-types\\.ts$",
      "reason": "`server/payload-types.ts` is generated by `pnpm types:cms` (it curls the schema from SahajCloud main and strips the module augmentation). Do not hand-edit it. To change CMS types the upstream schema must change and be merged first \u2014 see /cross-repo-issue. For app-specific types use `server/cms-types.ts`."
    },
    {
      "pattern": "^dist/",
      "reason": "`dist/` is build output from `pnpm build`. Change the source and rebuild."
    },
    {
      "pattern": "^pnpm-lock\\.yaml$",
      "reason": "`pnpm-lock.yaml` is generated by pnpm. Use `pnpm add` / `pnpm remove` / `pnpm update`."
    }
  ],
  "prAllowlistGlobs": [
    "package.json",
    "pnpm-lock.yaml",
    "**/*.md",
    ".claude/**",
    "server/payload-types.ts"
  ]
}
